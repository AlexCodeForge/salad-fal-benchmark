# Local mock inference stack — unified analyze service (see docker/analyze/Dockerfile).
services:
  analyze:
    build:
      context: .
      dockerfile: docker/analyze/Dockerfile
    ports:
      - "8001:8000"
    environment:
      MOCK_INFERENCE: "1"
